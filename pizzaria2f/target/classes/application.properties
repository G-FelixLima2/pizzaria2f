spring.application.name=pizzaria2f
server.port=8080
spring.datasource.url=jdbc:sqlserver://localhost;databaseName=pizzaria2f_db;integratedSecurity=false;encrypt=false;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=@ITB123456
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.hibernate.dialect=org.hibernate.dialect.SQLServer2012Dialect

# Informa��es:

# integratedSecurity:  (true)  Indica que a conex�o ser� feita com as credenciais do Windows (false) Credencial do SQL SERVER
# encrypt: (true) Requer criptografia do Cliente (false) N�o requer criptografia do Cliente
# trustServerCertificate: (true) Instrui o aplicativo cliente a ignorar a valida��o do certificado SSL/TSL do servidor

# spring.jpa.hibernate.ddl-auto:
# none: Banco de dados e tabelas devem ser criados com anteced�ncia
# update: Banco de dados criado com antecedencia, tabelas podem ser criadas com anteced�ncia, as tabelas s�o atualizadas pelo c�digo (model)
#         toda vez que vc cria novos atributos
# create-drop: Banco de dados deve ser criado com anteced�ncia, as tabelas s�o criadas automaticamente pelo c�digo (model), entretanto as
#              as tabelas s�o exclu�das toda vez que o servidor � "desligado"
# create: Banco de dados deve ser criado com anteced�ncia, quando o servidor carrega as tabelas s�o recriadas

# spring.jpa.show-sql= (true)   As intera��es (DDL,DML) com o banco de dados s�o exibidas no console
#                     (false)   As intera��es (DDL, DML) n�o ser�o exibidas no console
